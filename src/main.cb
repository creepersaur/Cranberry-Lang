class Vec {
	constructor(self, x, y) {
		self.x = x
		self.y = y
	}

	fn __Call__(self, scalar)
		=> Vec(self.x * scalar, self.y * scalar)

	fn __ToString__(self)
		=> $"({self.x}, {self.y})"
}

let v = Vec(3, 4)
println(v(2))   # → (6, 8)
